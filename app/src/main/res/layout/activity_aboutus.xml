<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    style="@style/CustomWidHieMatch">

    <WebView
        style="@style/CustomWidHieMatch"
        android:id="@+id/webView"/>

    <ImageButton android:id="@+id/closeButton"
        android:layout_width="@dimen/start_img"
        android:layout_height="@dimen/start_img"
        android:layout_gravity="top|end"
        android:background="@drawable/menu_close"
        android:contentDescription="close-button" android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true" android:layout_marginTop="15dp"
        android:layout_marginEnd="@dimen/activity_vertical_margin"/>
</RelativeLayout>